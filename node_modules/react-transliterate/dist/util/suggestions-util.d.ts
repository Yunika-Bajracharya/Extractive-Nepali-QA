import { Language } from "../types/Language";
declare type Config = {
    numOptions?: number;
    showCurrentWordAsLastSuggestion?: boolean;
    lang?: Language;
};
export declare const getTransliterateSuggestions: (word: string, config?: Config | undefined) => Promise<string[]>;
export {};
